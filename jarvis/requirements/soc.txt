# requirements/soc.txt — defensive SOC / DFIR / detection engineering stack.
#   pip install -r requirements/base.txt -r requirements/soc.txt
-r base.txt

yara-python>=4.5.0         # Layer 0 command scanner + file monitor
scapy>=2.5.0               # packet manipulation / sniffing (needs privileges)
paramiko>=3.4.0            # SSH automation
pefile>=2023.2.7           # PE triage
capstone>=5.0.1            # disassembly
stix2>=3.0.0               # STIX 2.1 IOC/TTP export
scipy>=1.12.0             # statistical anomaly detection
mmh3>=4.1.0               # correlation bucket hashing
feedparser>=6.0.12        # CISA RSS threat feeds
ntplib>=0.4.0             # forensic chrono-sync
piexif>=1.1.3             # EXIF/GPS forensics
cryptography>=42.0.0      # AES-GCM for RF-OOB, integrity
watchdog>=4.0.0           # hot-reload playbooks
networkx>=3.2.0           # topology / attack graphs
ijson>=3.2.3              # streaming JSON for large BloodHound exports
igraph>=0.11.0            # fast Dijkstra pathfinding
pyserial>=3.5            # RF/RFID COM enumeration
pyserial-asyncio>=0.6
asyncssh>=2.14.0         # native async Cisco IOS transport
netmiko>=4.3.0           # multi-vendor SSH fallback

# Persistent state + SIEM forwarding (TITAN). Optional services; modules
# degrade gracefully when Postgres/Redis are absent.
asyncpg>=0.29.0
redis>=5.0.0
