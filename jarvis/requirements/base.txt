# requirements/base.txt — minimal footprint for JARVIS TEXT MODE.
# Lets you run `python main.py` (text) and the doctor scripts without pulling
# audio, OCR, ML, or lab tooling. Heavier capabilities live in sibling profiles.

# ── Config / validation ────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── LLM (Ollama via OpenAI-compatible REST) ────────────────────────────────
openai>=1.0.0
httpx>=0.27.0

# ── Logging / CLI ──────────────────────────────────────────────────────────
loguru>=0.7.0
rich>=13.0.0

# ── Web + light tools (no API key) ─────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
duckduckgo-search>=6.0
dnspython>=2.6.0
python-whois>=0.9.0

# ── System / scheduling / async I/O ────────────────────────────────────────
psutil>=5.9.8
schedule>=1.2.0
PyYAML>=6.0.1
aiohttp>=3.9.3
aiofiles>=23.2.1
aiosqlite>=0.20.0

# ── MCP (Packet Tracer bridge, optional at runtime) ────────────────────────
mcp>=1.0.0
