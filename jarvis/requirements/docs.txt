# requirements/docs.txt — document reading / office formats / OCR.
# Powers read_file, leer_archivo_universal, create_document and screen OCR.
#   pip install -r requirements/base.txt -r requirements/docs.txt
-r base.txt

pdfplumber>=0.10.0
python-docx>=1.1.0
openpyxl>=3.1.0
pandas>=2.0.0
python-pptx>=0.6.23
Pillow>=10.0.0
pytesseract>=0.3.10        # needs the tesseract binary installed in the OS
easyocr>=1.7.1             # OS-independent OCR fallback (downloads model weights)
striprtf>=0.0.26
markdown>=3.5.0
